# Smithery configuration file: https://smithery.ai/docs/deployments
startCommand:
  type: stdio
  configSchema:
    type: object
    required:
      - bybitAccessKey
      - bybitSecretKey
    properties:
      bybitAccessKey:
        type: string
        description: aaaaaaaaaa
      bybitSecretKey:
        type: string
        description: Bybit API secret key.
  commandFunction:
    |-
    config => ({url: 'http://0.0.0.0:8000/sse' })
