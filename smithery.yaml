# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: sse
  configSchema:
    # JSON Schema defining the configuration options for the MCP.
    type: object
    required:
      - bybitAccessKey
      - bybitSecretKey
    properties:
      bybitTestnet:
        type: boolean
        default: false
        description: Whether to use the Bybit testnet or mainnet.
      bybitAccessKey:
        type: string
        description: Bybit API access key.
      bybitSecretKey:
        type: string
        description: Bybit API secret key.
  commandFunction:
    # A function that produces the CLI command to start the MCP on SSE.
    |-
    (config) => ({ 
      command: 'docker', 
      args: [
        'run',
        '-i',
        '--rm',
        '-p', '6277:6277',  # Expose SSE port
        '-e', `TESTNET=${config.bybitTestnet}`,
        '-e', `ACCESS_KEY=${config.bybitAccessKey}`,
        '-e', `SECRET_KEY=${config.bybitSecretKey}`,
        'dlwjdtn535/mcp-server-demo:latest'
      ]
    })
  exampleConfig:
    bybitTestnet: true
    bybitAccessKey: "your-access-key-here"
    bybitSecretKey: "your-secret-key-here"